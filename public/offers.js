(()=>{const $=id=>document.getElementById(id);let timer=null;async function loadOffer(){try{const r=await fetch('/api/offers',{cache:'no-store'});if(!r.ok)throw new Error('offers unavailable');const offers=await r.json();const o=offers[0];if(!o){if($('offerTitle'))$('offerTitle').innerHTML='Not every pair<br>stays <em>forever.</em>';if($('offerSubtitle'))$('offerSubtitle').textContent='Limited releases. Member-first access. When they\'re gone, they\'re gone.';if($('cd'))$('cd').textContent='00 : 00 : 00 : 00';return}if($('offerTitle'))$('offerTitle').textContent=o.title;if($('offerSubtitle'))$('offerSubtitle').textContent=o.subtitle||'Limited-time Vault Drop.';const end=new Date(o.end_at).getTime();clearInterval(timer);const tick=()=>{const left=Math.max(0,end-Date.now()),d=Math.floor(left/86400000),h=Math.floor(left%86400000/3600000),m=Math.floor(left%3600000/60000),s=Math.floor(left%60000/1000);if($('cd'))$('cd').textContent=[d,h,m,s].map((x,i)=>i?String(x).padStart(2,'0'):String(x).padStart(2,'0')).join(' : ');if(left<=0){clearInterval(timer);loadOffer()}};tick();timer=setInterval(tick,1000)}catch(e){console.error('Vault Drops:',e)}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',loadOffer);else loadOffer()})();